model Concert {
  id           String        @id @default(uuid())
  name         String
  description  String
  totalSeats   Int
  createdAt    DateTime      @default(now())
  reservations Reservation[]
}
